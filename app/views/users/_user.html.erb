<tr>
  <th>
    <%= gravatar_for user, size: 50 %>
  </th>
  <th>
    <%= link_to user.name, user %>
    <% if params[:manage] && current_user.admin? && user != current_user %>
      | <%= link_to "delete", user, method: :delete, data: { confirm: "Are you sure?" }, class: "danger-link" %>
    <% end %>
  </th>
</tr>